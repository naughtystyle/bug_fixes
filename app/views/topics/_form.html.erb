<%= form_with(model: topic, local: true) do |form| %>
  <div class="form-group">
    <%= form.text_field :name, id: :topic_name, class: "form-control", placeholder: "Enter topic name..." %>
  </div>

  <%= form.submit "Save", class: "btn btn-primary" %>
<% end %>
