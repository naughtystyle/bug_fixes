default: &default
  adapter: mysql2
  encoding: utf8
  reconnect: false
  pool: 5
  timeout: 5000
  port: 3306

development:
  <<: *default
  username: root
  password:
  host: 127.0.0.1
  database: tournament-development

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  username: root
  password:
  host: 127.0.0.1
  database: tournament-test
